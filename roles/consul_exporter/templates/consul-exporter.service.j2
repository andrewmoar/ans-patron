[Unit]
Description=consul metrics exporter for Prometheus
After=network-online.target

[Service]
Type=simple
User=root
Group=root
ExecStart=/opt/consul_exporter-0.4.0.linux-amd64/consul_exporter 
SyslogIdentifier=consul-exporter
Restart=always

[Install]
WantedBy=multi-user.target
